{
  "name": "ghl-mcp",
  "version": "2.0.0",
  "description": "GoHighLevel API v2 MCP server with full scope coverage (314 tools across 28 categories: contacts, conversations, calendars, opportunities, invoices, payments, products, social media, voice AI, and more).",
  "author": {
    "name": "Timi Abiola"
  },
  "mcpServers": {
    "ghl-api": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/index.js"],
      "env": {
        "GHL_API_KEY": "${GHL_API_KEY}",
        "GHL_LOCATION_ID": "${GHL_LOCATION_ID}"
      }
    }
  }
}
